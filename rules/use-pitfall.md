---
description: When something just went wrong mid-task, propose the /pitfall command — a user-invoked slash command, not a tool.
alwaysApply: true
---
The `/pitfall` command captures "something just went wrong" into `.omp/knowledge/pitfalls/` before the context fades. It is a user-invoked slash command — you have no tool for it. When a failure happens mid-task and the user wants to keep the lesson, propose `/pitfall`.
