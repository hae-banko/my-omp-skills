Run a `grilling` session: read the `grilling` skill and follow its loop — interview the user relentlessly, one question at a time, providing your recommended answer for each, until every branch of the decision tree is resolved.

While you grill, run the `domain-modeling` skill alongside: challenge fuzzy terms against the project's glossary, update `CONTEXT.md` inline as terms resolve, and offer an ADR when a decision is hard to reverse, surprising without context, and the result of a real trade-off.
